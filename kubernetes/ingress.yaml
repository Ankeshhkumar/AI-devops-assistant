apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
 name: ai-backend-ingress
spec:
 ingressClassName: nginx
 rules:
  - http:
      paths:
       - path: /
         pathType: Prefix
         backend:
           service:
             name: ai-backend-service
             port:
               number: 8000

